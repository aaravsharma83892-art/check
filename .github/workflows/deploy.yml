name: Deploy
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Inject Secret
        run: |
          sed -i 's/{{GROQ_API_KEY}}/${{ secrets.GROQ_API_KEY }}/g' index.html
      - name: Deploy to GitHub Pages
        # (Use a standard deployment action here)
